### **0x00 演讲开场：未来的未来**
* **对应时间**：`00:00 - 01:06`
* **PPT 内容简述**：
  * **标题**：0x00 Future of Future 未来的未来
  * **讲师信息**：王宏府（PSF 会员、Python 文档组成员）
  * **会议标识**：PyChina / PyCon China 2025 (AI For Good)

> **演讲实录**：
>
> “好，我觉得我作为一个中间的缓场吧。毕竟刚才 Victor 老师（或者前面讲底层 CPython 的老师）讲的会比较深一点。那我主要是负责文档方面的，所以说作为文档的话，我们其实是一个边缘成员，但边缘成员我们会更擅长去把一些东西让用户解释得懂。
>
> 所以我认为可以有更多的刚入门的爱好者，可以去了解一下这方面的知识。其实我前两年也已经做了一个铺垫，这个主题其实是我最早就像作为第一个主题来讲的，但是我最后其实准备了将近三年的时间。积累的资料量怎么说呢，反正超出了 100 万 token，丢给 Gemini 它也是解决不了的。
>
> 所以今天的时间是会比较紧的，我可能会用比较快的语速，大家如果感兴趣，可以后面看一下我其他之前的一些分享。还有我提前声明一下，因为主题确实有点大，时间比较紧，所以我们的讨论会不那么严谨，有些内容之间可能不那么连贯，是因为我可能已经删掉了非常多的内容。大家会看到我前面这 PPT 是一小时前最终定稿的，好，那我们就开始吧！”

---

### **0x10 什么是 `Future`？**
* **对应时间**：`01:07 - 02:14`
* **PPT 内容简述**：
  * **标题**：`0x10 What is 'Future'?`
  * **页面插图**：《攻壳机动队》笑脸男（Laughing Man）图案与风衣人物。
  * **核心图示**：异步模型时序图与状态机。生产者与消费者通过 `Future/Promise` 交互，状态机包含 `Pending`、`Fulfilled`（通过 `set_result`）、`Rejected`（通过 `set_exception`）。标注“不阻塞，继续执行”。

> **演讲实录**：
>
> “首先，既然提到了这个话题，那我们其实得考虑第一个问题：这里提到的 Future 有两个。第一个 Future 很简单，是‘未来’，我们去做展望；那第二个我们提到的 Future 是什么呢？
>
> 实际上，它应该是一个类，一个 Python 标准库中的类型，或者具体说是两个。那我们来具体了解一下 Python 中的 Future 的概念。
>
> 在了解 Python 之前，我们不妨先来看一看在通用的编程理念中，Future 是什么？
>
> 直接说到 Future，大家可能并不是那么熟悉。如果大家做过一些 JS 相关的操作，它有另外一个名字，叫做 `Promise`。它其实在 Python 里面是完全相同的东西，后面我们统一称为 Future。
>
> 那么 Future 实际上是一种设计模式，它真正代表的是一个什么东西呢？它是一个**占位符**。这个占位符代表着：我要做一件东西、做一件事，但这事还没有完成，但我想提前拿到做这件事情的一个结果的占位，或者说一个把手，好让我未来能把这个答案给拉回来，或者给推回来。
>
> 这一般代表着计算过程要消耗比较长的时间，所以我们要通过一个调用栈，提前拿到一个东西，防止它把我的逻辑停留在当前位置。”

---

### **0x11 占位符与代理对象**
* **对应时间**：`02:15 - 03:32`
* **PPT 内容简述**：
  * **标题**：`0x11 What is 'Future'?`
  * **内容标注**：异步处理的 占位符（代理对象）。
  * **流程图**：发起异步操作 $\rightarrow$ 返回 Future/Promise $\rightarrow$ 是否完成？（否：继续执行其他工作；是：交付结果/异常）。

> **演讲实录**：
>
> “那么我们简化一下它的描述，其实 Future 实际上就是一个**异步处理的占位符和代理对象**。
>
> 那它的这种代理对象的模式有什么好处呢？如果大家以前做过一些更早期的框架，或者在 Python 2 时代去做过一些异步，那你会知道，在以前的异步中我们的核心理念是：**无止境的回调**。
>
> 什么叫做回调呢？我给你做一件事，我希望你做完之后再通知我，这期间我可以做别的事。那我怎么做到呢？我丢给你一个把手，或者我留给你我的电话号码，你做完了之后你可以 call 我，我告诉你做完了。在现实里面就代表着大量的回调函数的传递。
>
> 第一层没有问题，第二层还能接受，第三层、第四层……当你的回调变得越来越复杂、你的监听事件变得越来越多的时候，这就出现了一个编程上非常著名的概念，叫做**回调地狱（Callback Hell）**。
>
> 也就是说，当我们的回调流程复杂且数量过多的时候，对于开发者来说它非常难以理解。那么怎么样去把回调封装起来呢？首先我们一定会需要一个对象，对吧？这个对象里面放了回调的东西，那我们未来如果有更多的东西，可以通过这个对象的叠加和它的继承把这个封装起来，这就是我们最终 Future 的概念。”

---

### **0x20 - 0x21 为什么 Python 里面有两个 `Future`？**
* **对应时间**：`03:33 - 06:56`
* **PPT 内容简述**：
  * **标题**：`0x20 Why double 'Future'?` & `0x21 Why double 'Future'?`
  * **插图**：明日香与绫波丽（双生子概念）。
  * **代码对比**：
    * `from concurrent.futures import Future`
    * `from asyncio import Future`
  * **类方法对比表**：列出两个类的成员方法，展示 `asyncio.Future` 比 `concurrent.futures.Future` 多了 `__await__`、`__iter__`、`get_loop` 等协程相关方法。

> **演讲实录**：
>
> “接下来我们可以谈一谈 Python 里面的 Future。实际在 Python 标准库中，有两个完全相同名称的 Future，并且它们非常相似，就像一枚硬币的两面，或者说是双生子。
>
> 为什么 Python 里面会出现两个 Future 呢？我们看一下这两个 Future 具体是什么：
> 第一个，是在 `concurrent.futures` 下面的；第二个，是在 `asyncio` 下面的。
>
> 它们分别的功能是什么？第一个大家可能不太熟悉，但大家对它下面的另一个东西比较熟悉：`ThreadPoolExecutor`、`ProcessPoolExecutor`。也就是说，它是我们通常 Python 封装得很好的、用来执行多线程和多进程任务底层的封装概念。在你不知道的时候，你用 Executor，底层就已经用到了 Future。
>
> 那第二个是在 `asyncio` 当中。这个可能小伙伴会有更多体会，因为 asyncio 是一个快速发展的库。在早期理念中，Future 是你不得不去创建但又非常棘手的一个底层概念。在后来的发展中我们不再直接用 Future，而是创建了一个东西叫 `Task`。我们在运行协程前，文档都会说把它包成一个 Task，而 Task 实际上不过是对 Future 的一个良好包装，在整体概念和具体分歧上它与 Future 并没有严格的区分。
>
> 我们再来看看它们的方法集对比。左边是 `concurrent.futures.Future`，右边是 `asyncio.Future`。看起来很明显，右边多了很多东西。而左右共同拥有的，比如 `add_done_callback`、`set_result`、`done`、`running` 等状态值操作。
>
> 而在右边 asyncio 中，最引人注意、最关键的两个方法是：`__await__` 和 `__iter__`。大家如果敏锐的话，就能感觉到这一定跟异步有关了：当我们使用关键字 `await` 的时候，它实际底层会调用双下划线方法 `__await__`。而在早期没有原生协程时，Python 协程是借用生成器实现的，这个 `__iter__` 就是迭代器的核心。显然右边的 Future 已经能看出协程相关的端倪了。”

---

### **0x22 调度机制对比：抢占式 vs 协作式**
* **对应时间**：`06:57 - 09:34`
* **PPT 内容简述**：
  * **标题**：`0x22 Why double 'Future'?`
  * **核心对比**：
    * **抢占式**：OS 内核（态）调度。OS 调度器（Scheduler）强制分配时间片，抢占切换，阻塞/就绪。
    * **协作式**：用户（态）事件循环调度。应用层事件循环（Event Loop），任务 A 遇到 `await` 主动让出运行权，事件就绪后恢复。

> **演讲实录**：
>
> “我们具体来看一下它们在使用过程中的差别在哪里。
>
> 左边，实际上是我们使用多线程、多进程调度时，这个异步调用的状态。现在的操作系统中，假设我们只有一个内核，它会把它切成一个个时间片。这很好理解，分时操作系统嘛，切一个足够短的时间片让你感知不到，在这个时间片执行完后切换到另一个线程，给你一种‘大家都在跑’的错觉。
>
> 在这个过程中，有一个很明显的区别：如果你看过操作系统内核代码，内核调度器有一个关键词叫做**抢占（Preemption）**。在 OS 内核看来，所有进程都非常重要，它希望让所有进程都尽可能去跑，不归属于某一个人。上一个人占着座不走，时间一到就一脚踢出去换人。
>
> 而右边是我们 asyncio 也就是协程里面的调度模式。从名字就能望文生义：第一点，它不再是抢占式的，它是**协作式（Cooperative）**的。
>
> 什么是协作式呢？你到底什么时候去执行下一个任务，并不是由下一个任务抢来的，而是由上一个任务**主动让出（Yield）**的。也就是说‘我现在不让出来，你下一个人绝对干不了’。
>
> 第二个区别是调度器承担者不同：传统多线程、多进程模型的调度器是由 OS 内核承担的，在操作系统层面实现；而在 asyncio 协程里，调度器是应用层的 Event Loop（事件循环），是在用户态实现的。
>
> 为什么这样做？asyncio 一直以来以高性能著称，为什么高性能？正是因为在用户态调度，**减少了对内核态的上下文切换开销**。这就是根本原理。”

---

### **0x23 并发 (Concurrency) vs 并行 (Parallelism)**
* **对应时间**：`09:35 - 11:42`
* **PPT 内容简述**：
  * **标题**：`0x23 Why double 'Future'?`
  * **对比表格**：
    * **并发**：核心是一段时间内处理多个任务；宏观同时、微观交替；单核或多核均可；关注资源复用和吞吐；适用于 I/O 密集型；生活例子：一位厨师做多道菜（交替切菜、炖汤）。
    * **并行**：核心是同一时刻处理多个任务；宏观微观都是物理上的同时；依赖多处理器；关注提升速度；适用于 CPU 密集型；生活例子：多位厨师做多道菜（每人各做一道）。

> **演讲实录**：
>
> “我们平常经常混用两个词：一个叫**并发（Concurrency）**，一个叫**并行（Parallelism）**。它们到底有没有区别呢？其实原则上是有的。
>
> 我们讲一个具体的例子：
> 对于并发来说，就像一位厨师要做多道菜，他交替做一些事情：比如水烧开的间隙他去切菜。在微观的任何一个瞬间，这位厨师其实只在干一件事，但是从宏观较长的时间维度来看，他同时完成了多道菜。
>
> 对于并行来说，这就很简单了：有多个厨师，每个人各自做各自的菜，他们是在物理时间上真正同时执行的。
>
> 从微观视角看，并发绝非同时执行，而是交替执行；并行则是绝对的同时发生。因此要实现绝对的并行，必须依赖硬件基础——多核处理器。单核处理器下所谓的并行只是假象，本质依然是并发。
>
> 映射到适用场景：并发对应 **I/O 密集型任务**（网络、文件读写等），而并行对应 **CPU 密集型任务**（大规模计算、图形渲染等）。”

---

### **0x30 - 0x31 什么是协程？**
* **对应时间**：`11:43 - 15:05`
* **PPT 内容简述**：
  * **标题**：`0x30 What is 'coroutine'?` & `0x31 What is 'coroutine'?`
  * **人物相片**：David Beazley 及其经典演讲封面《A Curious Course on Coroutines and Concurrency》 (PyCon 2009)。
  * **系统图示**：OS 中断与 Trap 流程、任务队列（Ready Queue / Wait Queue）。

> **演讲实录**：
>
> “谈完 Future，我们重点来看看这个 Future 背后所代表的 Python 核心概念——**协程（Coroutine）**。
>
> 屏幕上这位老哥叫 David Beazley，他在 Python 领域是泰斗级的人物。正是他的演讲启发了我做这一整套主题。我极其强烈推荐大家去读一读他 2009 年在 PyCon 上的演讲《A Curious Course on Coroutines and Concurrency》。
>
> 我们先回顾一下没有协程之前，传统操作系统线程是如何运行的。运行到一个阶段，程序会通过 `trap`（陷入）或者中断将控制权交给 OS 内核，内核把任务放到等待队列或就绪队列，进行任务切换。
>
> 在这种模式下，微观上每个任务都被切成极细的时间片，由内核充当总指挥，不断在就绪队列与等待队列间轮转，这就是典型的抢占式并发。”

---

### **0x32 全局解释器锁 (GIL)**
* **对应时间**：`15:06 - 17:34`
* **PPT 内容简述**：
  * **标题**：`0x32 What is 'coroutine'?`
  * **核心内容**：CPython Process 中多线程（Thread A, B, C）竞争 Global Interpreter Lock (GIL)。
  * **流程分支**：
    * CPU-bound $\rightarrow$ 性能瓶颈，无真正并行。
    * I/O-bound $\rightarrow$ 等待 I/O 时释放 GIL，实现并发。

> **演讲实录**：
>
> “聊到 Python 的并发多线程，大家一定会遇到一个名词：**GIL（Global Interpreter Lock，全局解释器锁）**。
>
> 它实际上是 CPython 解释器的一个特定实现机制。简单来说，在执行 Python 字节码时，GIL 保证同一时刻只有一个线程拥有执行权。不管你的机器有几个核，也不管你开了多少个线程，它们都得竞争同一把锁，抢到锁的才能跑，抢不到的只能等。
>
> 为什么要加这把看似自我受限的锁？因为自由是建立在限制之上的。有了基础的解释器级限制，我们读写内置对象（如 list）就天然线程安全，无需频繁在 Python 代码层面加繁重的细粒度锁，反而提升了单线程运行性能。
>
> 但它的弊端显而易见：对于 CPU 密集型任务，多线程不仅无法利用多核并行，甚至因为反复的争锁上下文切换，耗时比单线程还要长！
>
> 唯一能够绕开 GIL 限制的，就是 **I/O 密集型任务**。因为当线程执行网络、磁盘 I/O 等待时，CPython 会主动释放 GIL，允许其他线程运行。”

---

### **0x33 - 0x36 asyncio 与 I/O 多路复用**
* **对应时间**：`17:35 - 21:13`
* **PPT 内容简述**：
  * **标题**：`0x33 What is 'coroutine'?` - 标有 “asyncio”，红字突出 “IO”。
  * **核心机制**：`0x34 IO 多路复用`。
  * **架构对比**：`0x35` select/poll ($O(n)$) vs epoll/kqueue ($O(1)$)。
  * **实现机制**：`0x36 Coroutine Trampolining`。通过事件循环与协程蹦床机制调度。

> **演讲实录**：
>
> “既然有 GIL 的限制，为什么 asyncio 却能实现极高的并发性能？
>
> 关键就在于它的名字：`asyncio`，重点在后面的 **IO** 上。它的核心支撑并不是 Python 创造了什么魔法，而是它深度利用了现代操作系统内核赋予的一项通用能力——**I/O 多路复用（I/O Multiplexing）**。
>
> 就像 Nginx 一样，假设同时有 3 个用户访问服务器读写数据，单线程不可能同时处理。传统的 select/poll 机制效率较低，是 $O(n)$ 的轮询扫描；而现代操作系统的 epoll（Linux）和 kqueue（macOS/BSD）做到了 $O(1)$ 的事件通知机制。
>
> 操作系统内核充当了‘大堂经理’，单线程只需把成千上万个连接托管给内核监听，当某个连接有数据就绪时，内核主动唤醒 Event Loop 过来处理。
>
> asyncio 正是在底层巧妙地封装了系统的 I/O 多路复用，再通过类似 Trampoline（蹦床机制）的事件循环，将繁琐的底层系统调用优雅地包装成高级语法，从而在单线程下支撑起数以万计的高并发网络连接。”

---

### **0x37 - 0x38 协程理论的深度与历史**
* **对应时间**：`21:14 - 22:25`
* **PPT 内容简述**：
  * **插图**：动漫人物恶搞图。
  * **知识点罗列**：计算续体（Continuation）、生成器协程 `yield from`、有栈/无栈（Stackful vs Stackless）、对等/不对等（Symmetric vs Asymmetric）。
  * **趣味名言**：“我有一段很精彩的演讲，但是这里写不下了。——WH-2099”。

> **演讲实录**：
>
> “如果要真正深入协程，它涵盖了计算机科学中极其广阔的领域：从上世纪六七十年代的计算续体（Continuation），到生成器协程 `yield from`、有栈与无栈协程的选型、对称与非对称协程……
>
> 我借用费马大定理的名言改写一下：‘我有一段很精彩的演讲，但是这里写不下了’。如果大家做深入研究，建议仔细研读相关学术文献和 David Beazley 的系列分享，这会极大提升你对协程与操作系统内核的底层理解。”

---

### **0x40 - 0x44 编程哲学思考：永远不要使用 `goto` 吗？**
* **对应时间**：`22:26 - 32:19`
* **PPT 内容简述**：
  * **书籍封面**：《C语言从入门到精通》。
  * **文献考据**：
    * 狄克斯特拉经典论文：《A Case Against the Goto Statement》（Edsger W. Dijkstra）及被编辑沃斯（Niklaus Wirth）改名为著名的《Go To Statement Considered Harmful》（1968）。
    * 高德纳反驳论文：《Structured Programming with go to Statements》（Donald Knuth）。
  * **代码示例**：展示 Linux 内核中经典的 `goto out;` 统一资源释放结构。
  * **核心概念解析**：结构化编程（Structured Programming）的核心是**单一入口与单一出口**（Single entry, single exit），旨在使“代码的阅读顺序与执行顺序保持一致”，降低认知负载。

> **演讲实录**：
>
> “我们换个轻松一点但同样深刻的话题：大家入门学 C 语言时，书里几乎都会有一条铁律——‘**永远不要使用 goto**’。
>
> 这句话到底对不对？它是怎么来的？
> 追溯到 1968 年，著名的计算机先驱 Dijkstra 发表了一封通信信件，原文标题很委婉：《A Case Against the Goto Statement》。当时 ACM 的主编沃斯（Niklaus Wirth，Pascal 之父）为了吸引眼球，擅自把标题改成了火药味十足的《Go To Statement Considered Harmful》（Goto 语句有害论）。这成了计算机史上有名的‘标题党’引发的历史误会。
>
> Dijkstra 的本意并不是判处 `goto` 死刑，而是反对**毫无节制地滥用 goto 导致难以维护的‘面条式代码（Spaghetti Code）’**。
>
> 事实上，在高德纳（Donald Knuth）著名的论文《Structured Programming with go to Statements》以及我们至今看到的 Linux 内核源码中，`goto` 依然被高频、合理地用于底层统一的错误处理和资源清理（如统一释放内存、关闭文件描述符），这不仅高效而且极度清晰。
>
> 这场论战沉淀下来的真正瑰宝，是**结构化编程（Structured Programming）**。结构化编程的精髓，是用 `if`、`loop`、`function call` 确立了代码块的**单一入口和单一出口**，让程序员的阅读逻辑和机器的执行路径重新归于一致。这一哲学在日后被深深烙印进了所有现代高级编程语言中。”

---

### **0x50 - 0x53 结构化并发 (Structured Concurrency)**
* **对应时间**：`32:20 - 40:58`
* **PPT 内容简述**：
  * **标题**：`0x50 Structured Concurrency`（配以动画《EVA》SEELE 标志）。
  * **时序与机制图**：
    * `0x50`：无序并发与结构化并发时序对比（左边协程逃逸、生命周期失控；右边父子生命周期严格嵌套）。
    * `0x51`：TaskGroup 时序交互图与植物大战僵尸坚果墙插图。
  * **Python 语法落地**：`0x52` 展示 Python 3.11 引入的 `class asyncio.TaskGroup`；`0x53` 展示标准用法：
    ```python
    async def main():
        async with asyncio.TaskGroup() as tg:
            task1 = tg.create_task(some_coro())
            task2 = tg.create_task(another_coro())
        print(f"Both tasks have completed now: {task1.result()}, {task2.result()}")
    ```

> **演讲实录**：
>
> “历史总是惊人地相似。半个多世纪前我们通过结构化编程干掉了面条式的控制流；而近些年，面对日益繁杂的异步并发，我们又陷入了另一种‘面条式并发’：
>
> 在传统的并发编程中（无论是 Go 还是早期 Python），你在函数里随手启动一个协程，它就‘野蛮生长’地跑到了外部，生命周期完全不可控。主函数退出了，子协程可能还在后台漏跑；子协程崩了，父协程根本感知不到，导致资源泄漏和难以调试。
>
> 2016 年左右由社区提出的**结构化并发（Structured Concurrency）**，正是将半个世纪前结构化编程的哲学，移植到了并发领域！
>
> 它的核心原则是：**并发任务必须有严格的父子层级树形作用域，必须拥有明确的单一生命周期入口与出口。子任务的生命周期绝对不能超越其父作用域。**
>
> 在 Python 3.11 中，官方终于正式落地了这一理念的实现——`asyncio.TaskGroup`。
>
> 通过上下文管理器 `async with asyncio.TaskGroup() as tg:`：
> 1. 在该作用域内派生出的所有异步任务，当退出上下文代码块时，会隐式等待（join）所有子任务全部完成；
> 2. 如果其中任何一个子任务发生未捕获异常，`TaskGroup` 会自动且级联地取消组内所有其他子任务；
> 3. 这种级联取消杜绝了悬空协程和野指针式的幽灵任务，使异步程序的错误处理和资源回收重新变得如同步代码般优雅、可读、可预测。”

---

### **0x60 - 0x64 Python 并发的终极展望：noGIL 与未来生态**
* **对应时间**：`40:59 - 46:39`
* **PPT 内容简述**：
  * **标题**：`0x60 Future of Future`（紫禁城角楼守卫配图）。
  * **PEP 提案梳理**：
    * **PEP 684**：A Per-Interpreter GIL（Python 3.12 引入子解释器独立 GIL）。
    * **PEP 703**：Making the Global Interpreter Lock Optional in CPython（Python 3.13 自由线程/noGIL 实验性支持）。
    * **PEP 734**：Multiple Interpreters in the Stdlib（标准库子解释器支持）。
  * **演进对比**：回顾 2018 年 John Reese 提出的过渡库 `aiomultiprocess`（结合 asyncio 与进程池）。
  * **内省监控工具**：`0x63` & `0x64` 展示 Python 3.14+ 即将带来的 `asyncio` 原生内省能力：
    * 命令：`python -m asyncio ps PID` / `python -m asyncio pstree PID`
    * 终端可视化输出：树状层级展示所有运行中的协程调用关系及阻塞等待状态。

> **演讲实录**：
>
> “掌握了上面的所有底层与哲学基石后，我们终于可以来到真正的‘未来的未来’：
>
> 这么多年来，Python 开发者为了在 GIL 存在的情况下榨干多核算力，想尽了各种招数。比如 2018 年 John Reese 做的著名开源库 `aiomultiprocess`，硬生生把进程池与 asyncio 揉合在一起，本质上是妥协的产物。
>
> 但现在，时代彻底变了！从 Python 3.12 到 3.14，我们迎来了 CPython 历史上最波澜壮阔的变革：
> 1. **PEP 684 & PEP 734（子解释器）**：每个解释器拥有独立的 GIL，允许在单个操作系统进程内并发运行真正隔离的多解释器；
> 2. **PEP 703（noGIL / Free-threaded Python）**：从 Python 3.13 开始，GIL 成为可选配置，官方开始正式支持完全移除 GIL 的多线程物理并行版本！
>
> 当限制被彻底打破，Python 不仅保留了在 I/O 密集型领域凭借事件循环带来的高吞吐，更将具备在 CPU 密集型任务上直接利用多线程实现真并行的能力。
>
> 随着结构化并发对协程调度的规范化，Python 3.14 甚至进一步提供了像操作系统般的原生**内省（Introspection）**能力：
> 我们未来可以直接通过命令行输入 `python -m asyncio pstree <PID>`，无需对代码打入侵式断点，就能在终端清晰看到当前进程内所有协程以层级树的形式展开——谁在 sleep、谁被阻塞在哪个网络调用上一目了然！这对于大型分布式系统的调试与观测是革命性的生产力释放。”

---

### **结语与赞助/交流环节**
* **对应时间**：`46:40 - 47:07`
* **PPT 内容简述**：
  * **赞助企业**：深圳某聚焦 AI Agent 硬件创业团队的招募信息。
  * **致谢页**：演讲者终端交互博客 `ssh starv.ing` 与个人博客 `blog.wh2099.com`。

> **演讲实录**：
>
> “最后总结我今天的演讲：**杀不死我的，只会让我变得更强大**。
>
> 正是由于 CPython 过去存在着 GIL 的硬性约束，才倒逼了整个社区在单线程异步、I/O 多路复用和结构化并发的道路上探索得如此深入，创造了无与伦比的优雅设计。而当今天 GIL 的枷锁终于被官方解除时，带着成熟结构化并发体系的 Python，将在高性能计算和并发系统领域迎来前所未有的广阔空间。
>
> 感谢大家听完我这场稍微有点超时的演讲，最后放一下赞助方的招募信息和我的联系方式，大家如果想在终端一起聊 Python，可以通过 `ssh starv.ing` 或者访问我的博客交流。谢谢大家！”
