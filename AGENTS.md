# PyCon China 2026 上海场演讲项目协作约定

本约定适用于本项目的全部协作与交付。

## 协作原则

- 把演讲质量放在机械执行之前。
- 主动、广泛地搜索、探寻、核对与校验材料，不把草稿中的表述默认视为事实。
- 优先引用 PEP、官方文档、标准原文、论文和项目源码等一手资料。
- 明确区分已核实事实、合理推断、个人观点与尚待确认的信息，并记录核验日期。
- 发现失真、歧义、逻辑跳跃、偏题或结构松散时，主动指出并提出更准确的替代方案。
- 对重要观点进行交叉核验；若权威来源之间存在差异，保留差异而不是强行给出单一结论。
- 与讲者共同讨论，不盲从命令，也不擅自改变演讲立场。
- 当前阶段只建设资料、引用、工具和工程基础，不直接续写或定稿演讲内容。

## 内容基调

- 演讲定位是对当天相关议题的总结与升华，主线是 Python 并发从"能不能"走向"怎样做好"。
- 内容偏概论，但保留决定结论的关键技术细节；宁可少讲，也不牺牲准确性。
- 使用费曼式表达，让具备基础教育背景的听众能理解问题、演进逻辑和核心取舍。
- 风格诙谐、从容，有生活化类比与小段子，但笑点不能替代论证或弱化技术准确性。
- 设计问句、留白、停顿和音乐等节奏变化，避免持续单向灌输。
- 讲稿、幻灯片和现场演示都应服务同一条叙事主线，而不是堆砌技术名词。

## 工程原则

- 遵循第一性原理与 KISS 原则，优先从根因解决问题，拒绝临时补丁。
- 优先使用最新且稳定的技术栈；积极重构，无需维持本项目早期草稿或实验接口的向前兼容。
- 先复用项目现有实现，再考虑标准库、平台原生能力和已有依赖；不为假想需求搭建抽象。
- Python 环境与依赖使用 `uv` 管理，任务命令使用 `justfile`，不创建 `Makefile`。
- `pyproject.toml` 只保留本项目实际使用的依赖与格式化配置，不引入 pytest 等当前无关工具。
- Markdown 偏好每行一句，并同时使用 Ruff 与 rumdl 维护 Python 和 Markdown 格式。
- 每次提交前必须先运行格式化，再运行完整检查；检查至少覆盖 justfile、uv 锁文件、Ruff、rumdl、ty 和 marimo。
- ty 用于类型检查而非格式化，但属于提交前的必过门禁。
- 前端或浏览器行为的实现与判断，先查询 MDN Web Docs。
- 优先使用 `anysearch` 搜索；不可用时依次使用 `rg` 等现有工具。
- 使用 `jaq` 而不是 `jq`，`fd` 而不是 `find`，`rg` 而不是 `grep`，`choose` 而不是 `cut`。
- `/data/git` 下存在相关裸仓时优先复用，并在使用前更新远端引用。

## 资料与资产

- 明确提到的 PEP 必须从官方原文转换并归档为 Markdown，同时记录来源与转换方式。
- 相关但正文未直接提到的关键 PEP 或文献也应纳入引用，并说明与哪一部分相关。
- 论文只做一次简单获取与 Markdown 转写；若权威来源无法直接下载，记录待手动补齐项，不为此引入复杂抓取方案。
- 下载资料保留稳定、可追溯的文件名；同时维护来源 URL、标题、状态、用途和获取日期索引。
- 机械转写的外部原文可以禁用 Markdown 风格修复，避免格式化器改变公式、表格或来源结构；自编索引仍须通过 rumdl。
- 背景图片逐张记录内容、尺寸、适用场景、文字安全区、可读性及来源或版权待确认项。
- 音频、图片和演示依赖均需考虑会场离线运行、浏览器权限、自动播放限制和故障降级。

## Git 与协作平台

- 处理 Issue 或 PR 时先尝试 self-assign。
- branch、commit 与 PR 标题严格遵循 Conventional Commits。
- 保留用户已有改动；不覆盖、不删除来源不明的内容。
