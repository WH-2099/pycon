# PyCon China 2026 上海场项目结论

核验日期：2026-09-04。

本文件只保留当前已经形成的结论、风险和执行约束，不是演讲正文或新版大纲。

## 会议信息与定位

- 大会日期为 2026 年 9 月 5 日，主题为 `AI for Good`，地点为上海对外经贸大学古北校区综合楼。
- 演讲题目是《让我们自由自在地并发》，讲者为王宏府，公开副标题是《对并发运行时、数据共享边界与结构化并发范式演进的系统性探讨》。
- 演讲时间为 14:50–15:30，地点为分会场 A（综合楼 506 报告厅），是茶歇前最后一场，但不是当天或该会场的闭幕演讲。
- 同场此前已有 GIL 入门、自由线程迁移和 CPython 贡献等内容，本场适合作概念收束，但不能假设观众听过前序演讲。
- 对外摘要已经承诺覆盖 CPython 运行时、标准库、编程范式、子解释器、自由线程、PEP 805、`bocpy` 与声明式并发；新版大纲可以取舍细节，但不能悄然更换议题。
- 主线是"执行能力释放以后，如何重建秩序"，而不是罗列 Python 并发技术。
- 正文使用三个正交问题组织内容：任务怎样被组织、任务怎样真并行、任务怎样共享状态。
- 与 `AI for Good` 的连接应落在"能力与责任一起进入设计"，并明确这是讲者观点。
- "计算自由，数据有界"计划只在全场最终总结时出现；前文负责完成论证，不提前把结论口号化。
- 第四章负责论证状态的访问边界与同步保护，最终口号仍只在全场收束时完整出现。

当前讲稿统一维护在项目根目录的 [最终讲稿.md](最终讲稿.md)，五章已合并到同一文件。

初版草稿标称需要 63–76 分钟，正文约 3.1 万字，不能直接作为 40 分钟现场稿。

第五章将三个问题汇总到"让关系进入运行时"的展望，BOC 与 `bocpy` 是其中一条探索路线。

新版大纲应先为每段确定唯一 takeaway，再按时间预算取舍；一张 slide 只承担一个命题。

## 必须守住的事实边界

- 生成器和协程组织暂停与协作式控制流，不等于实现 CPU 并行。
- `Future.cancel()` 通常不能强制终止已经运行的工作。
- `TaskGroup` 主要组织生命周期、取消和异常传播，不决定 CPU 调度策略。
- 子解释器、每解释器 GIL 和自由线程都必须注明 Python 版本、构建方式与运行配置。
- Free-threaded 构建不代表运行时一定关闭 GIL，GIL 也从未保证复合业务操作原子。
- 共享同一块内存不自动保证同步、原子性或可见性。
- `bocpy` 根据显式声明的 Cown 请求建立依赖，不会理解任意 Python 数据访问。
- PEP 703、734、779、803 已 Final，PEP 790 为 Active。
- 这些 PEP 分别约束可选 GIL、多解释器 API、自由线程支持阶段、`abi3t` 和 Python 3.15 发布计划。
- PEP 789、603、805 当前为 Draft，PEP 583 已 Withdrawn，PEP 797 已 Rejected。
- PEP 814 已 Final，但目标 Python 3.15 在核验日尚未正式发布；所谓 PEP 795 仍只是 PR 草案。
- 已发布能力、Draft PEP、第三方实现、研究原型和讲者设想必须分层呈现。
- 新机制通常与线程、锁、进程和消息传递长期共存，不应讲成线性淘汰史。

## Marimo 方案

- 项目锁定 marimo 0.24.0，已验证 Reveal slides、Markdown、HTML、图片、视频和本地音频。
- 第一页固定为暖场页：封面背景、大字 `AI` 与"爱"的节拍切换，以及《爱》的空格播放与手动控件。
- 当前按 126 BPM 设计为四拍停留加一拍转场，每次转场占一拍，即 476.190476 ms。
- 动画相位直接跟随音频播放时间，因此首次播放、暂停或跳转都不会让视觉时钟自行漂移。
- 第二页是单张动态标题页：第一次按空格突出 `for Good`，第二次按空格进入主标题、副标题和作者信息。
- 标题页由讲者手动掌握停留时间，不跟随音乐 BPM；系统要求减少动态效果时保留相同步骤并取消过渡。
- 第一章梗图页将两张 EVA 图片并排展示，不另设标题，并单独使用接近整个白色主体的排版框。
- 第二至第四章各有一张同构过渡页，内容取自第一章末表格；第一次空格聚焦当前章，第二次空格进入下一页。
- "自由之后"的总结与展望各自独立成页，保留左侧竖排标题，分别用生成图收束三项责任和提出运行时关系的开放问题。
- 最后一页使用协作背景，以两块独立白色底板分别承载收束文案与博客二维码，避免覆盖右侧插画。
- 博客二维码编码完整地址 `https://blog.wh2099.com`，使用 H 级纠错和四模块白色静区，并保留可读域名作为兜底。
- 暖场页第一次按空格播放音乐，第二次按空格进入标题页；使用原生控件播放同样视为第一次触发。
- 进入最后一页 5 秒后，答谢页播放器会从头以 10% 音量播放一次《爱》；空格会取消计时或暂停音乐，原生控件仍可播放、暂停和调节音量，离页会取消计时、停止播放并复位。
- 波形分析得到这份录音的稳定网格约为 126.117 BPM；若要求整曲严格贴合录音，应在现场预演后再校准拍长。
- 编辑使用 `just edit`，现场使用 `just present` 启动仅绑定本机的离线服务。
- 默认单元格使用常规背景，名称以 `cover` 或 `collaboration` 开头时自动切换对应版式。
- 背景与内容框位于同一个 16:9 Reveal 坐标平面，内容超量时保留 marimo 原生滚动提示。
- 0.24.0 的静态 HTML 不保留完整 slide 体验且依赖网络资源，不能作为离线灾备。
- PDF 只能作为失去音频、交互和动画后的二级灾备。
- 演讲者视图可按 `S` 打开，必须提前验证双屏、弹窗和窗口归属。

## 背景与媒体

三张背景均为 1920 × 1080，百分比顺序为 `left / top / width / height`。

| 背景 | 排版框 | CSS `inset: top right bottom left` | 用途 |
| --- | --- | --- | --- |
| [`bg-cover.png`](public/bg-cover.png) | `30% / 29% / 65% / 57%` | `29% 5% 14% 30%` | 开场与结尾 |
| [`bg-content.png`](public/bg-content.png) | `3% / 15% / 78% / 79%` | `15% 19% 6% 3%` | 常规内容 |
| [`bg-collaboration.png`](public/bg-collaboration.png) | `3% / 4% / 69% / 82%` | `4% 28% 14% 3%` | 协作、生态与问答 |

- 封面排版框刻意纳入下方空间，左下及右下积木属于需要逐页手工避让的特例。
- 动态标题页为后排视线单独放宽并上提排版框，使用 `inset: 24% 6% 30% 28%`。
- 协作背景的点阵可能产生摩尔纹，正文使用 CSS 已配置的 96% 不透明白色底板。
- 三张图都要在真实投影、直播录屏和 16:9 全屏下复测裁切、字号、对比度与摩尔纹。
- 大会官方隐喻是"内容是积木"，信封或拼图的具体含义只能作为讲者解释。
- 背景图的修改、公开入库和随录像传播权限仍需向主办方确认。
- 三张 EVA 衍生图片的公开演讲、直播和录像传播权限也需要单独确认。

[`ai.flac`](public/ai.flac) 在暖场页由第一次空格或原生控件播放，避免依赖浏览器自动播放授权。

答谢页的 5 秒延时播放仍可能受浏览器策略影响，原生控件保留为现场兜底。

离开暖场页时 Reveal 会自动暂停音频。

音乐必须人工触发并准备独立播放器；还需确认换场时间、AV 接口、公播授权和录像版权。

## 现场与下一步

- 完成一次飞行模式冷启动和一次全新浏览器 profile 预演。
- 在最终电脑、浏览器、投影分辨率和音响链路上验证页面、字体、背景、音频和 speaker view。
- 向主办方确认讲者信息、换场缓冲、图片权限、音乐播放及直播录像授权。
- 第二至第四章已按三个问题重构，后续复核继续统一标注 PEP 状态、目标版本和实现层级。
- 所有可运行示例都必须在目标 Python 与依赖版本上重新验证。

PEP、论文与 Python 文档原文统一保存在 [`ref/`](ref/)。

## 关键来源

- 大会：[上海场日程](https://cn.pycon.org/2026/schedule/)、[演讲页面](https://cn.pycon.org/2026/talks/let-concurrency-be-free/)、[视觉设计说明](https://github.com/PyConChina/PyConChina2026/blob/main/DESIGN_2026.md)。
- Python：[PEP 索引](https://peps.python.org/)、[PEP 789](https://peps.python.org/pep-0789/)、[PEP 805](https://peps.python.org/pep-0805/)、[PEP 795 PR 草案](https://github.com/python/peps/pull/4468)。
- marimo：[0.24.0 发布记录](https://github.com/marimo-team/marimo/releases/tag/0.24.0)、[slides 文档](https://github.com/marimo-team/marimo/blob/0.24.0/docs/guides/apps.md)、[WASM 限制](https://docs.marimo.io/guides/wasm/)。
- BOC：[`bocpy` 文档](https://microsoft.github.io/bocpy/)、[原始论文](https://doi.org/10.1145/3622852)、[因果模型论文](https://doi.org/10.4230/LIPIcs.CONCUR.2026.23)。
- 媒体：[MDN 自动播放指南](https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Autoplay)。
